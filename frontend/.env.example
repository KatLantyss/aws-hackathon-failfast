# Copy to .env and fill in real values. Never commit .env.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional overrides
ANTHROPIC_MODEL=claude-sonnet-5
OPENAI_STT_MODEL=gpt-4o-transcribe

# Backend API base URL (deployed Ship Performance Analysis API - sls-api Lambda,
# see sls-api/API.md). No trailing slash; endpoints are appended as /health,
# /api/v1/vessels, etc.
VITE_BACKEND_BASE_URL=https://4rh4qj5e3i.execute-api.us-east-1.amazonaws.com/dev
